
using BBS.Controllers;
using BBS.Models;
using BBS.Services;
using Microsoft.AspNetCore.Mvc;
using Moq;
using NUnit.Framework;
using System;
using System.Collections;
using System.Collections.Generic;
using System.Linq;
using System.Net.Http;
using System.Text;
using System.Threading.Tasks;

namespace BBS
{
    [TestFixture]
    public class TestTopicReplyClass
    {
        [Test]
        public void TestMethod()
        {
        //TopicController controller = new TopicController();
        //    Reply reply = new Reply();
        //    reply.Content = "123";
        //    reply.AddTime = DateTime.Now;
        //    reply.IsTopic = TopicOrReply.Topic;
        //    reply.LastTime = reply.AddTime;
        //    reply.TopicId = "123";
        //    ViewResult result = controller.Index(reply) as ViewResult;
        //    Assert.IsNotNull(result);
        }
    }
}
